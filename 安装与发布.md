# 安装与发布

这套插件采用“一个 GitHub 仓库，安装两次”的结构：

- 酒馆网页安装一次：负责设置页、召回预览和生成前注入。
- VPS 的 `plugins` 目录安装一次：负责保存人物状态、检索和备份。

两份代码来自同一个仓库，但人物记忆只保存在 VPS 后端副本的 `data` 目录中。

## 一、上传 GitHub

1. 登录 GitHub，新建空仓库 `ST-Character-Continuity`。
2. 不要勾选自动创建 README、`.gitignore` 或许可证。
3. 复制仓库的 HTTPS 地址，格式类似：

   `https://github.com/你的用户名/ST-Character-Continuity.git`

4. 在本项目目录依次执行：

   ```powershell
   git add .
   git status
   git commit -m "Initial SillyTavern plugin"
   git remote add origin https://github.com/你的用户名/ST-Character-Continuity.git
   git push -u origin main
   ```

执行 `git add` 后必须检查：列表里不应出现 `data/state.json`、API Key、密码、
聊天原文或导出的状态备份。

## 二、安装 VPS 后端

先生成新的 SillyTavern 备份，确认备份路径和大小后再继续：

```bash
cd /opt/sillytavern/docker
backup_file="/root/sillytavern-before-character-continuity-$(date +%Y%m%d-%H%M%S).tar.gz"
tar -czf "$backup_file" config data plugins docker-compose.yml
ls -lh "$backup_file"
```

确认备份完成后，把仓库克隆到服务端插件目录：

```bash
cd /opt/sillytavern/docker
git clone https://github.com/你的用户名/ST-Character-Continuity.git plugins/character-continuity
docker compose restart
```

检查启动日志：

```bash
docker logs --since 2m sillytavern 2>&1 | grep -E "Character Continuity|plugin"
```

应当看到：

```text
[Character Continuity] 后端已启动
```

本插件没有第三方运行依赖，不需要执行 `npm install`。

## 三、安装酒馆前端

1. 打开 SillyTavern。
2. 进入“扩展”或“管理扩展”。
3. 选择“安装扩展”。
4. 粘贴同一个 GitHub 仓库地址。
5. 安装后刷新酒馆页面。
6. 点击酒馆顶部“扩展”图标，在扩展菜单中找到“人物连续性记忆”。
7. 打开全屏工作台，在“设置与备份”中点击“检查后端”。

显示版本号和批次数，说明前端与 VPS 后端已经接通。

## 四、第一次启用

1. 从本地人物记忆实验室导出状态 JSON。
2. 打开人物记忆工作台，在“设置与备份”中点击“导入状态”。
3. 填写与状态文件一致的“故事”和“时间线”。
4. 在“召回预览”中运行一次预览，确认人物、成长和关系正确。
5. 最后才开启“生成前自动召回并注入”。

第一轮建议使用：

```text
最近消息：8
画像上限：4
成长上限：6
关系上限：8
最大注入字符：6000
```

## 五、更新

前端和后端是两份安装，需要分别更新：

- 前端：在酒馆扩展管理器中点击更新。
- 后端：

  ```bash
  cd /opt/sillytavern/docker/plugins/character-continuity
  git pull --ff-only
  cd /opt/sillytavern/docker
  docker compose restart
  ```

更新前先在插件里“导出备份”。不要删除后端插件目录后重新克隆，否则其
`data/state.json` 也会一起消失。
